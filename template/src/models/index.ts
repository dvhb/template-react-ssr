// You can include shared models/types in a separate file
// and then use them in any component by importing them. For
// example, to import the interface below do:
//
// import User from 'path/to/models';

export type User = {
  id: number;
  name: string;
};
